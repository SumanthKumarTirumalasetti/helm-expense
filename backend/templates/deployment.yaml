kind: Deployment
apiVersion: apps/v1
metadata:
  name: backend
  namespace: expense
  labels:
    app: backend
    tier: backend
    project: expense
  
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
      tier: backend
      project: expense

  template:
    metadata:
      labels:
        app: backend
        tier: backend
        project: expense
    spec:
      containers:
      - name: backend
        image: sumanth7696/backend:{{ .Values.deployment.image }}
